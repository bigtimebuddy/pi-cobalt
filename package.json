{
  "name": "cobalt-calendar-app",
  "version": "1.0.0",
  "description": "Report times when Cobalt poops, pees, walks or feeds",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "onoff": "^1.1.2"
  }
}
